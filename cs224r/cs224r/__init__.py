"""
Compatibility package.

The homework starter code in this repo lives at the top-level (e.g. `agents/`,
`critics/`, `infrastructure/`), but the provided scripts import modules under the
`cs224r.*` namespace. This package bridges those layouts by extending each
submodule search path to point at the legacy top-level directories.
"""

